# 1 Module
A module is a file containing code written by someboyd else(usually) which can be imported and used in our program

- ex. fileName : CModule.py
- ```py
  def add(a,b):
      print(a+b)
  ```

- import CModule.py
  ```py
  import CModule
  CModule.add(5,5)   # Output 10
  ```
